language: cy
responses:
  intents:
    HassGetState:
      default: "{{ state.state_with_unit }}"

      one: |
        Mae {{ slots.name | capitalize }} yn {{ state.state_with_unit }}

      one_yesno: |
        {% if query.matched %}
          Ydy
        {% else %}
          Nac ydy, {{ state.state_with_unit }}
        {% endif %}

      any: |
        {% if query.matched %}
          {% set match = query.matched | map(attribute="name") | sort | list %}
          {% if match | length > 4 %}
            Oes, {{ match[:3] | join(", ") }} a {{ (match | length - 3) }} yn rhagor
          {%- else -%}
            Oes,
            {% for name in match -%}
              {% if not loop.first and not loop.last %}, {% elif loop.last and not loop.first %} a {% endif -%}
              {{ name }}
            {%- endfor -%}
          {% endif %}
        {% else %}
          Nac oes
        {% endif %}

      all: |
        {% if not query.unmatched: %}
          Ydyn
        {% else %}
          {% set no_match = query.unmatched | map(attribute="name") | sort | list %}
          {% if no_match | length > 4 %}
            Nac ydyn, nid yw {{ no_match[:3] | join(", ") }} a {{ (no_match | length - 3) }} yn rhagor yn {{ slots.state }}
          {%- else -%}
            Nac ydyn,
            {% for name in no_match -%}
              {% if not loop.first and not loop.last %}, {% elif loop.last and not loop.first %} a {% endif -%}
              {{ name }}
            {%- endfor %} {% if no_match | length > 1 %}ddim{% else %}ddim{% endif %} yn {{ slots.state }}
          {% endif %}
        {% endif %}

      which: |
        {% if not query.matched %}
          Dim un
        {% else: %}
          {% set match = query.matched | map(attribute="name") | sort | list %}
          {% if match | length > 4 %}
            {{ match[:3] | join(", ") }} a {{ (match | length - 3) }} yn rhagor
          {% else %}
            {%- for name in match -%}
              {% if not loop.first and not loop.last %}, {% elif loop.last and not loop.first %} a {% endif -%}
              {{ name }}
            {%- endfor -%}
          {% endif %}
        {% endif %}

      how_many: |
        {{ query.matched | length }}

      where: |
        {% if not query.matched %}
          Mae'n ddrwg gennyf, dydw i ddim yn ymwybodol o unrhyw berson o'r enw {{ slots.name | capitalize }}
        {% else: %}
          {% if state == "not_home" %}
            Mae {{ slots.name | capitalize }} i ffwrdd
          {% else %}
            Mae {{ slots.name | capitalize }} yn {{ state.state }}
          {% endif %}
        {% endif %}
