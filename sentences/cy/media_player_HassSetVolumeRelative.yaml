language: cy
intents:
  HassSetVolumeRelative:
    data:
      - sentences:
          - "(troi (y sain;(i fyny|i lawr)) [{volume_step_up}|{volume_step_down}];[ar] <name>)"
          - "(cynyddu|gostwng) [y] sain [{volume_step_up}|{volume_step_down}] [ar] <name>"
        requires_context:
          domain: media_player
      - sentences:
          - "troi (y sain;(i fyny|i lawr)) [{volume_step_up}|{volume_step_down}]"
          - "(cynyddu|gostwng) [y] sain [{volume_step_up}|{volume_step_down}]"
        requires_context:
          area:
            slot: true
      - sentences:
          - "troi (y sain;(i fyny|i lawr)) [{volume_step_up}|{volume_step_down}] [yn|ym] <area>"
          - "(cynyddu|gostwng) [y] sain [{volume_step_up}|{volume_step_down}] [yn|ym] <area>"
