language: cy
intents:
  HassTurnOn:
    data:
      - sentences:
          - "cloi <name> [<yn> <area>]"
        requires_context:
          domain: lock
        response: lock

      - sentences:
          - "cloi [pob] <cloiadwy> [<yn>] <area>"
          - "cloi [pob] <area> [<cloiadwy>]"
          - "cloi pob <cloiadwy>"
        slots:
          domain: "lock"
        response: lock
